version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "5000:5000"
    volumes:
      - /home/sirin/BIGDATA:/data
    environment:
      - NODE_ENV=production
      - REACT_APP_API_URL=http://localhost:5000
    container_name: bigdata-file-display
